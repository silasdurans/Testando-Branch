# projeto-site

Estrutura simples de site estático de exemplo.

Arquivos principais:

- `index.html` — página inicial
- `about.html` — página Sobre
- `contact.html` — página Contato
- `styles.css` — estilos
- `scripts.js` — script mínimo
- `assets/` — imagens e recursos

Como testar localmente (com Python 3):
